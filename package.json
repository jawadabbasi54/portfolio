{
  "name": "jawad-abbasi-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "verify:build": "node scripts/verify-build.mjs",
    "assets:land-mask": "node scripts/generate-land-mask.mjs",
    "test:e2e": "playwright test",
    "quality": "npm run check && npm run build && npm run verify:build"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.55.1",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3"
  }
}
